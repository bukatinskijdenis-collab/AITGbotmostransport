#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import telebot
from gtts import gTTS
import os
import requests
import time
import logging
import sys
import uuid
import urllib3

# --- Отключение SSL-предупреждений (self-signed certificate) ---
urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

# --- Логирование ---
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[logging.StreamHandler(sys.stdout)]
)
logger = logging.getLogger("GigaChatBot")

# --- Конфигурация ---
TELEGRAM_BOT_TOKEN = "8251896486:AAFb9SBoI_TKjIbYrFgMiUTPPbJo1dJu-Xw"

# Данные из SberStudio - CLIENT_SECRET уже содержит закодированные credentials
AUTHORIZATION_KEY = "MDE5OWU3ODYtN2Q3Yy03NjlhLTg5YjQtN2YwODZjMTlkZTc1OjQ0MzE5M2YzLTkxZjctNDI0YS05YWY5LWQ1NGY3OWY2ZmU3Mw=="

OAUTH_URL = "https://ngw.devices.sberbank.ru:9443/api/v2/oauth"
CHAT_URL = "https://gigachat.devices.sberbank.ru/api/v1/chat/completions"
MODELS_URL = "https://gigachat.devices.sberbank.ru/api/v1/models"

bot = telebot.TeleBot(TELEGRAM_BOT_TOKEN)

access_token = None
token_expires = 0


# ====== Получение access_token ======
def get_gigachat_token():
    global access_token, token_expires

    headers = {
        'Content-Type': 'application/x-www-form-urlencoded',
        'Accept': 'application/json',
        'RqUID': str(uuid.uuid4()),
        'Authorization': f'Basic {AUTHORIZATION_KEY}'
    }

    payload = {
        'scope': 'GIGACHAT_API_PERS'
    }

    try:
        logger.info("🔄 Получение токена GigaChat...")
        response = requests.post(
            OAUTH_URL,
            headers=headers,
            data=payload,
            verify=False,
            timeout=30
        )

        logger.info(f"📊 Статус ответа: {response.status_code}")

        if response.status_code == 200:
            token_data = response.json()
            access_token = token_data.get("access_token")
            expires_in = token_data.get("expires_in", 1800)
            token_expires = time.time() + expires_in - 60

            if access_token:
                logger.info(f"✅ Access token получен, действителен {expires_in} сек.")
                return True
            else:
                logger.error("❌ Токен не найден в ответе")
                return False
        else:
            logger.error(f"❌ Ошибка получения токена: {response.status_code}")
            logger.error(f"📄 Ответ сервера: {response.text}")
            return False

    except Exception as e:
        logger.error(f"❌ Ошибка сети при получении токена: {e}")
        return False


def is_token_valid():
    return access_token and time.time() < token_expires


# ====== Проверка доступности моделей ======
def check_models_availability():
    """Проверяет доступность моделей GigaChat"""
    if not is_token_valid():
        if not get_gigachat_token():
            return False

    headers = {
        'Accept': 'application/json',
        'Authorization': f'Bearer {access_token}'
    }

    try:
        logger.info("🔄 Проверка доступности моделей...")
        response = requests.get(
            MODELS_URL,
            headers=headers,
            verify=False,
            timeout=30
        )

        if response.status_code == 200:
            models_data = response.json()
            logger.info(f"✅ Модели доступны: {len(models_data.get('data', []))} моделей")
            return True
        else:
            logger.error(f"❌ Ошибка проверки моделей: {response.status_code}")
            return False

    except Exception as e:
        logger.error(f"❌ Ошибка при проверке моделей: {e}")
        return False


# ====== Запрос к GigaChat ======
def ask_gigachat(prompt: str) -> str:
    global access_token

    # Проверяем и обновляем токен если нужно
    if not is_token_valid():
        logger.info("🔄 Токен истек или отсутствует, запрашиваем новый...")
        if not get_gigachat_token():
            return "❌ Не удалось получить токен GigaChat"

    headers = {
        'Content-Type': 'application/json',
        'Accept': 'application/json',
        'Authorization': f'Bearer {access_token}'
    }

    payload = {
        "model": "GigaChat",
        "messages": [
            {
                "role": "user",
                "content": prompt
            }
        ],
        "temperature": 0.7,
        "max_tokens": 1024
    }

    try:
        logger.info("🔄 Отправка запроса к GigaChat...")
        response = requests.post(
            CHAT_URL,
            headers=headers,
            json=payload,
            verify=False,
            timeout=60
        )

        logger.info(f"📊 Статус ответа GigaChat: {response.status_code}")

        if response.status_code == 200:
            data = response.json()
            try:
                answer = data["choices"][0]["message"]["content"]
                logger.info("✅ Ответ от GigaChat получен")
                return answer
            except (KeyError, IndexError) as e:
                logger.error(f"❌ Ошибка парсинга ответа: {e}")
                return "❌ Ошибка: некорректный формат ответа GigaChat"

        elif response.status_code == 401:
            logger.warning("🔄 Токен невалиден, пробуем обновить...")
            if get_gigachat_token():
                headers["Authorization"] = f"Bearer {access_token}"
                response2 = requests.post(CHAT_URL, headers=headers, json=payload, verify=False, timeout=60)
                if response2.status_code == 200:
                    data2 = response2.json()
                    return data2["choices"][0]["message"]["content"]
            return "❌ Ошибка авторизации GigaChat"

        elif response.status_code == 429:
            return "❌ Превышен лимит запросов. Попробуйте позже."

        else:
            logger.error(f"❌ Ошибка API: {response.status_code}")
            return f"❌ Ошибка GigaChat API: {response.status_code}"

    except Exception as e:
        logger.error(f"❌ Ошибка сети при запросе к GigaChat: {e}")
        return f"❌ Ошибка сети: {e}"


# ====== Telegram бот ======
@bot.message_handler(commands=['start', 'help'])
def start(message):
    bot.send_message(
        message.chat.id,
        "👋 Привет! Я голосовой консультант GigaChat.\n"
        "Просто напиши любой вопрос, и я отвечу текстом и голосом!"
    )

    # Проверяем подключение при старте
    if get_gigachat_token():
        bot.send_message(message.chat.id, "✅ Подключение к GigaChat установлено!")
    else:
        bot.send_message(message.chat.id, "❌ Не удалось подключиться к GigaChat")


@bot.message_handler(commands=['test'])
def test_connection(message):
    """Тест подключения к GigaChat"""
    if get_gigachat_token():
        bot.send_message(message.chat.id, "✅ Токен успешно получен!")
        if check_models_availability():
            bot.send_message(message.chat.id, "✅ Модели GigaChat доступны")
        else:
            bot.send_message(message.chat.id, "⚠ Проблемы с доступом к моделям")
    else:
        bot.send_message(message.chat.id, "❌ Не удалось получить токен")


@bot.message_handler(func=lambda message: True)
def handle_message(message):
    user_text = message.text

    if not user_text or user_text.strip() == "":
        bot.send_message(message.chat.id, "📝 Пожалуйста, введите текст сообщения.")
        return

    processing_msg = bot.send_message(message.chat.id, "⏳ Думаю над ответом...")

    try:
        answer = ask_gigachat(user_text)

        # Удаляем сообщение "Думаю над ответом..."
        try:
            bot.delete_message(message.chat.id, processing_msg.message_id)
        except:
            pass

        # Отправляем текстовый ответ
        bot.send_message(message.chat.id, f"💬 Ответ:\n{answer}")

        # Озвучивание ответа
        try:
            bot.send_chat_action(message.chat.id, 'record_voice')

            # Ограничиваем длину текста для TTS
            tts_text = answer[:500] if len(answer) > 500 else answer

            audio_file = f"voice_{message.message_id}_{int(time.time())}.mp3"
            tts = gTTS(text=tts_text, lang='ru', slow=False)
            tts.save(audio_file)

            with open(audio_file, "rb") as voice:
                bot.send_voice(message.chat.id, voice, caption="🎧 Аудиоответ")

            # Удаляем временный файл
            if os.path.exists(audio_file):
                os.remove(audio_file)

        except Exception as e:
            logger.error(f"❌ Ошибка при озвучивании: {e}")

    except Exception as e:
        logger.error(f"❌ Общая ошибка обработки сообщения: {e}")
        bot.send_message(message.chat.id, "❌ Произошла ошибка при обработке запроса")


# ====== Инициализация при запуске ======
def initialize_bot():
    """Инициализация бота при запуске"""
    logger.info("🚀 Инициализация бота...")

    # Пробуем получить токен при старте
    if get_gigachat_token():
        logger.info("✅ Бот успешно инициализирован")
    else:
        logger.warning("⚠ Не удалось получить токен при старте")


# ====== Запуск ======
if __name__ == "__main__":
    initialize_bot()
    logger.info("🚀 Бот запущен и слушает сообщения...")

    try:
        bot.polling(none_stop=True, interval=1, timeout=60)
    except Exception as e:
        logger.error(f"❌ Критическая ошибка бота: {e}")
        sys.exit(1)
